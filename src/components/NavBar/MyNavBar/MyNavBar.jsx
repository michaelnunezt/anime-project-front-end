import './MyNavBar.css';  // Ensure you create a separate CSS file for styles

const MyNavBar = () => {
  return (
    <nav className="custom-navbar">
  <div>
    <span className="navbar-brand">PopPlay</span>
  </div>
</nav>
  );
};

export default MyNavBar;